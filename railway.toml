[build]
builder = "NIXPACKS"

[deploy]
startCommand = "prisma db push --skip-generate --accept-data-loss && tsx scripts/seed-entities.ts && npm start"
healthcheckPath = "/api/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
